# TJRJ Precatórios Extractor App - Dependencies
# Install: pip install -r app/requirements.txt

# Streamlit
streamlit>=1.28.0

# Extras for animations (confetti, balloons)
streamlit-extras>=0.3.0

# Data processing (already in main requirements.txt)
pandas>=2.0.0

# Browser automation (already in main requirements.txt)
playwright>=1.40.0

# Logging (already in main requirements.txt)
loguru>=0.7.0
